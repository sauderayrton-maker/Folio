{
    "name": "folio",
    "version": "1.1.0",
    "private": true,
    "description": "Dev tooling only — the app itself is plain HTML/CSS/JS with no build step.",
    "scripts": {
        "test": "playwright test",
        "serve": "python3 -m http.server 8000"
    },
    "devDependencies": {
        "@playwright/test": "^1.45.0"
    }
}
